<% if comment.id %>
<div class="comment">
	<p><%= comment.content %></p>
	<%= link_to comment_path(comment), method: :delete, data: { confirm: "Are you sure?" }, class: "delete", remote: true do %>
		<span class="glyphicon glyphicon-remove"></span>
	<% end %>
</div>
<% end %>
